import React from 'react';


const Calendar = (props) => {

    // Eventually, we will use this ID to load the calendar from DB
    calendarID = props.id;

    return(
        // For now, return a grid of Tile components
        <View />
    );
}

export default Calendar;